version: '3.8'

services:
  sipeadmin:
    build: .
    container_name: sipeadmin-app
    restart: unless-stopped
    ports:
      - "127.0.0.1:5001:5001"
    env_file:
      - ./backend/.env
    extra_hosts:
      - "host.docker.internal:host-gateway"
