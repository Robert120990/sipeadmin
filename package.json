{
  "name": "sipeadmin-root",
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf public && cd frontend && npm install && npm run build && cp -a dist ../public && cd ../backend && npm install"
  }
}
